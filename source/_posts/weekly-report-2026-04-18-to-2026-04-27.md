---
title: 使用周报 - 2026-04-18 ~ 2026-04-27
date: 2026-04-27
tags:
  - weekly
  - insights
categories:
  - Weekly Log
---

### 验证器之间谁也不验谁

这周「说通过」但有一层没验的事出现了 6 次：

- 04-18：Opus 自查 TL;DR 字数两轮翻车，靠 stdlib 正则脚本兜底才稳住
- 04-20：Zflow PR #47 的 api-base.ts 覆盖率 100%，但断言写的是错代码行为，dev mode 全 API 走 SPA fallback 假 200
- 04-21：子 agent 在同一条消息里报「全流程成功」又写「附带 404」
- 04-22：Sonnet F3 review 的 env.pop 建议被直接下笔，45 秒后因 _load_dotenv 守卫回滚
- 04-26：walking skeleton 的 spec / adapter / 单测 / probe 四层一致性集体失守，写 spec 那刻就埋下 `claude --cwd` 非法 flag
- 04-27：agent-sandbox 透明代理大重构合并后 verify.sh 八条断言一次没跑

六次用的判断器各不相同：Opus 自查、coverage 数字、子 agent 报告、Sonnet F3 review、四层一致检查、verify.sh。这六个判断器之间谁也不验谁。

→ 下周给 PR 描述加一栏：本次依赖的下游验证器是哪个，有没有实际跑过。

---

### 写进去的规则没有被用到的机制

memory 规则这周写进去的速度明显快于被用到的速度：

- 04-22：当天坑精确对应 feedback_evidence_before_guess、subagent_review_gate、parallel_planning_first 三条，当场没一条被想起来
- 04-23：harness 在每次 Edit memory 后自动注入 originSessionId，37 个文件的原始锚点被本次 UUID 不可恢复覆盖；刚写的「去引用化规则」被 harness 自身吃掉
- 04-24：「认知未落到代码位置就不算交付」是当天思考结论，但 slotted-deliberation skill v3 仍在 .claude/ gitignored，没升格到仓库根 skills/
- 04-27：Opus 凭记忆写 --cwd、没跑 verify.sh、GITHUB_TOKEN 透传 sandbox——三件都是现存 feedback memory 应当拦下的

每写一条规则只是在加文件数量。规则在被需要那一刻有没有被读到、写进去的规则有没有被 harness 机制反噬，这两件事目前没人追。

→ 下周可以在 daily-report 加一节：今天本可被现存 memory 拦下、但实际没拦下的事。

---

### 软追问翻案的对错取决于有没有带新证据

附一条：软追问的处理这周继续出错。

- 04-21：SkVM 三条建议评估时，Sentixxx 两次软追问，Opus 就改口把 AOT Pass3 DAG 和 JIT-boost beforeLLM 短路进候选池；派子 agent 核验后抓出 3 处事实偏差
- 04-22：Sonnet F3 review 的 env.pop 建议被直接下笔，45 秒后回滚

但：

- 04-24：防污染 hook 从补丁层提到根因层（PR #4，schema 物化目录后 hook 简化），这次翻案是对的
- 04-26：ADR-0008 多轮「还有更好方案」逼出根因，也是对的

区别在 Sentixxx 有没有带新证据进来。两次 SkVM 追问没有新证据——Opus 立场稳不稳、它会不会自己换思路，Sentixxx 在等答案。PR #4 和 ADR-0008 那两次带了新角度，翻案有据。feedback_judgment_drift_under_pushback.md 早有这条规则，但 04-21 和 04-22 都没生效。
